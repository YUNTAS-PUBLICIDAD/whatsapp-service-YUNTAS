services:
  whatsapp-service:
    build: .
    container_name: whatsapp-service
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./logs:/app/logs
      - ./auth_info:/app/auth_info
    environment:
      - NODE_ENV=production
      - PORT=3001